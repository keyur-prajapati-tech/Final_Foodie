@model Foodie.Models.DeliveryPartner.tbl_deliveryPartnerDetails

@{
    ViewData["Title"] = "Personal Details";
    Layout = "~/Views/Shared/_DeliverRegisterLayout.cshtml";  // Safe casting
}
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Arial', sans-serif;
    }

    body {
        background: linear-gradient(135deg, #e3f2fd, #e1bee7);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 0;
    }

    @@keyframes fadeInUp {
        from

    {
        opacity: 0;
        transform: translateY(20px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
    
    }

    .card {
        background: #e3e7ff;
        backdrop-filter: blur(10px);
        width: 400px;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.1);
        text-align: center;
        color: black;
        animation: fadeInUp 0.8s ease-in-out;
        margin-left: 50px;
    }

    .card-header h3 {
        font-weight: bold;
        color: #333;
        margin-bottom: 15px;
    }

    .form-control {
        width: 100%;
        padding: 12px;
        margin-top: 5px;
        margin-bottom: 15px;
        border: 1px solid #aaa;
        border-radius: 6px;
        font-size: 16px;
        background: rgba(255, 255, 255, 0.6);
        color: black;
    }

    .btn-primary {
        background-color: #007bff;
        color: white;
        border: none;
        padding: 12px;
        width: 100%;
        border-radius: 6px;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease-in-out;
    }

        .btn-primary:hover {
            background-color: #0056b3;
            box-shadow: 0px 0px 10px rgba(0, 123, 255, 0.4);
            transform: scale(1.05);
        }

    .form-label {
        color: #333;
        font-size: 14px;
        font-weight: bold;
        text-align: left;
        display: block;
    }
</style>

<div class="card">
    <div class="card-header">
        <h3>🚴‍♂️ Delivery Partner Details</h3>
    </div>
    <div class="card-body">
        <form asp-action="Per_Details" asp-controller="DeliverSignup" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label>Upload Profile Picture</label>
                <input type="file" name="profileImage" class="form-control" />
            </div>

            <input type="hidden" asp-for="ProfilePicture" />
            <div class="form-group">
                <label>Aadhaar Number</label>
                <input asp-for="AadhaarNumber" class="form-control" />
            </div>
            <div class="form-group">
                <label>PAN Number</label>
                <input asp-for="PANNumber" class="form-control" />
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</div>
